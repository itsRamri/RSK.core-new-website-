{
  "name": "rsk-portfolio-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx -y vite --open",
    "build": "echo \"Vercel Static Ready\"",
    "preview": "npx -y serve .",
    "serve": "npx -y serve ."
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
